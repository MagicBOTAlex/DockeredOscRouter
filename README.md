# Dockered OSC router
This is supposed to be use with the rest of my dockered VRCFT setup. Quick and quite vibe coded OSC router, that is docker compose based. I use it to repeat OSC packets to both my Windows set and Linux setup.

## Usage
Check the example `compose.yml` file
